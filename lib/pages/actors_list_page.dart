import 'package:flutter/material.dart';

class ActorsListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Actors List'),
      ),
      body: Center(
        child: Text('Actors List Page'),
      ),
    );
  }
}
